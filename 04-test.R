df_midterm <- data.frame(product = c("apple","strewbarry","watermelon"),
                         price = c(1800,1500,3000),
                         volume = c(24,38,13))

df_midterm

mean(df_midterm$price)
mean(df_midterm$volume)
